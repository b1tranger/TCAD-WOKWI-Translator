# Implementation Plan - Project Structure Setup

Establish the core project structure, documentation, changelog tracking, service worker, and application scaffolding according to [AGENTS.md](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/AGENTS.md) and the project specification, using `versions.md` instead of `changes.json`.

## User Review Required

> [!IMPORTANT]
> - `versions.md` replaces `changes.json` as requested. The in-app changelog modal will fetch and parse `versions.md` dynamically into readable changelog entries.
> - `AGENTS.md` will be updated to reflect `versions.md` instead of `changes.json`.
> - The application is designed as a zero-dependency, pure vanilla HTML5/CSS3/JavaScript web app with dark/sky-blue aesthetic and PWA offline capability via `sw.js`.

## Proposed Changes

### Configuration & Documentation

#### [MODIFY] [AGENTS.md](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/AGENTS.md)
- Update Rule 1 to replace references to `changes.json` with `versions.md`.

#### [NEW] [versions.md](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/versions.md)
- Markdown-based version history and release notes starting with `v0.1.0` (Initial Project Structure & Scaffolding).
- Formatted with structured headings and tags for clean in-app rendering.

#### [NEW] [doc/DOCUMENTATION.md](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/doc/DOCUMENTATION.md)
- Complete technical documentation containing:
  - Architectural overview (Vanilla stack, client-side execution, Tinkercad to Wokwi translation pipeline).
  - Table of Contents.
  - Component models and data schemas (Native JSON schema, Tinkercad structure, Wokwi `diagram.json` schema).
  - SVG breadboard canvas rendering specification and standalone universal HTML export engine.
  - `## Version History` section at the end of the file with high-level summaries as required by `AGENTS.md`.

#### [MODIFY] [README.md](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/README.md)
- Update with full project overview, badges, version `v0.1.0`, architecture summary, feature list, roadmap checklist, and quick start guide.

---

### Core Application Scaffolding

#### [NEW] [sw.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/sw.js)
- Service Worker with `const CACHE_VERSION = 'v0.1.0';` at line 5 (per rule requirement).
- Cache-first strategy for offline PWA operation with static asset pre-caching.

#### [NEW] [manifest.json](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/manifest.json)
- Web Application Manifest for installable PWA support with theme colors matching the dark/sky-blue palette.

#### [NEW] [index.html](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/index.html)
- Main application interface with:
  - Header toolbar with file actions (Open Tinkercad, Import JSON/HTML, Export Wokwi, Export Standalone HTML), zoom controls, and modals trigger.
  - Component sidebar / drawer with draggable parts (Breadboards, Arduino Uno, Resistor, LED, Potentiometer, Pushbutton).
  - SVG/Canvas interactive workspace with coordinate grid and breadboard layout.
  - Side inspector / translation preview pane (toggling between Wokwi `diagram.json` and Native JSON).
  - Modals: Changelog modal (powered by `versions.md`), Import URL modal, and Settings modal.

#### [NEW] [css/style.css](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/css/style.css)
- Premium dark/sky-blue aesthetic styling:
  - CSS custom properties (`--bg-primary: #090d16`, `--bg-surface: #0f172a`, `--accent-sky: #38bdf8`, `--accent-blue: #0284c7`, `--border-color: #1e293b`).
  - Sleek glassmorphic panels, responsive flex/grid layouts, crisp typography, and micro-animations.

#### [NEW] [js/app.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/js/app.js)
- Main app entry point, PWA service worker registration, UI event wiring, and state initialization.

#### [NEW] [js/schema.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/js/schema.js)
- Project schemas, pinout maps for Arduino Uno and breadboard tie-points, and default project templates.

#### [NEW] [js/translator.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/js/translator.js)
- Conversion pipelines:
  - Tinkercad JSON -> Native Project Schema
  - Native Project Schema -> Wokwi `diagram.json`
  - Standalone single `.html` exporter and importer

#### [NEW] [js/renderer.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/js/renderer.js)
- Interactive SVG circuit renderer for breadboard grid, Arduino board, components, and jumper wires with bezier curves.

#### [NEW] [js/ui.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/js/ui.js)
- UI controller managing toolbars, drag-and-drop component placement, inspector updates, and `versions.md` markdown parsing for the changelog modal.

---

## Verification Plan

### Automated / Syntax Verification
- Validate JSON structures (`manifest.json`) using node / JSON parser.
- Validate JavaScript syntax and module integrity.
- Check service worker line 5: ensure `const CACHE_VERSION = 'v0.1.0';` is exact.

### Manual Verification
- Verify that `index.html` loads cleanly without console errors in a browser.
- Verify that clicking the "Changelog" button loads and formats `versions.md` in the modal dialog.
- Verify dual-write prompt files in `doc/prompts/` conform to `AGENTS.md` guidelines.
