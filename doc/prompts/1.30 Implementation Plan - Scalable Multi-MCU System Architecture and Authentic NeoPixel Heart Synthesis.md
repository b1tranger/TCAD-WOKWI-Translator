# Scalable Multi-MCU System Architecture & Authentic NeoPixel Heart Synthesis

## User Review Required

> [!IMPORTANT]
> - The user specified that the system must handle **multiple Arduino boards** (not limited to 3 or 4, but any arbitrary count $N \ge 1$ required by any new or existing design), with scalable inter-MCU wiring, non-overlapping coordinate generation, and robust multi-source netlist compilation.
> - We will implement a generalized Multi-MCU architecture across ingestion, translation, rendering, and interaction, while using HLModtech's 4-Arduino NeoPixel Heart circuit as the prime benchmark.

---

## Proposed Changes

### 1. Multi-MCU Architecture & Component Scaling

#### [MODIFY] [js/schema.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/js/schema.js)
- Register `neopixel-strip` in `COMPONENT_CATALOG`:
  - Name: `"NeoPixel LED Strip (8 Pixels)"`, category: `"Outputs"`, `wokwiType: "wokwi-neopixel-strip"`, width: 140, height: 28.
- Register `neopixel-ring` in `COMPONENT_CATALOG`:
  - Name: `"NeoPixel Ring (12 Pixels)"`, category: `"Outputs"`, `wokwiType: "wokwi-neopixel-ring"`, width: 64, height: 64.

#### [MODIFY] [js/app.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/js/app.js)
- Enhance `addComponent`:
  - Implement dynamic multi-MCU spacing: when adding multiple boards of the same or different microcontroller types (Arduino Uno, Mega, Nano, ESP32), calculate a smart grid/spiral coordinate offset so boards never overlap.
  - Increment unique sequential identifiers (e.g. `arduino_uno_1`, `arduino_uno_2`, `arduino_uno_3`...).

---

### 2. Multi-MCU Netlist & Wokwi Compilation

#### [MODIFY] [js/translator.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/js/translator.js)
- **Arbitrary Multi-MCU Support in `Translator.toWokwi`**:
  - Preserve every microcontroller board in `parts` with distinct IDs (`arduino_1`, `arduino_2`, ..., `arduino_N`).
  - Support multi-MCU inter-board connections:
    - Direct board-to-board wires (`arduino_1:3` $\rightarrow$ `arduino_2:2`, `arduino_master:A4` $\rightarrow$ `arduino_slave:A4`, common ground `arduino_1:GND.1` $\rightarrow$ `arduino_2:GND.1`).
    - Multi-source breadboard nets: handle multiple Arduinos sharing common power/ground rails or I2C busses without short-circuiting or dropping slave boards.
- **Pin Normalization**:
  - Add `wokwi-neopixel-strip` and `wokwi-neopixel-ring` pin mappings (`DIN`, `DOUT`, `5V`, `GND`) in `normalizeWokwiPin`.
- **Authentic Valentine Contest Synthesis**:
  - Implement full 4-Arduino Uno distributed architecture:
    - `arduino_master` (bottom left, $x: 310, y: 750$)
    - `arduino_slave_1` (middle left upper, $x: 110, y: 460$)
    - `arduino_slave_2` (middle left lower, $x: 110, y: 580$)
    - `arduino_slave_3` (top right, $x: 870, y: 170$)
    - `power_supply` (top left, $x: 160, y: 150$)
    - `bb_main` (bottom center, $x: 420, y: 720$) with potentiometer and pushbutton
    - `lcd_i2c` (heart center, $x: 430, y: 320$) with `"Happy Valentine! <3"`
    - 6 NeoPixel strips + 3 NeoPixel rings forming the geometric heart outline.
    - Full inter-MCU sync, I2C, power, and data wiring.
- **Standalone HTML Export**:
  - Update `exportStandaloneHTML` and its dynamic renderer to support NeoPixel strips, rings, and multi-MCU layouts.

---

### 3. Canvas Viewport Auto-Fit & Multi-MCU Bezier Routing

#### [MODIFY] [js/renderer.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/js/renderer.js)
- Implement `getNeopixelStripSVG(comp)`:
  - Matte black PCB, 8 surface-mount 5050 RGB LEDs with silk-screen indicators and solder pads (`DIN`, `5V`, `GND`, `DOUT`).
- Implement `getNeopixelRingSVG(comp)`:
  - Circular PCB ring with perimeter array of 12 SMD RGB LEDs and solder pads.
- Add pin coordinate resolvers for `neopixel-strip` and `neopixel-ring`.
- Add `fitToContent(project)`:
  - Computes the bounding box of all components across large multi-board circuits and centers the viewport with an optimal zoom scale (e.g. 0.65–0.8x for large multi-MCU setups).
- Add dynamic bezier arc calculation for long inter-board wires to prevent wire overlapping.

---

### 4. Cache and Documentation Synchronization

#### [MODIFY] [sw.js](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/sw.js)
- Bump `CACHE_VERSION = 'v0.1.16'`.

#### [MODIFY] [index.html](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/index.html)
- Bump version badge and asset query strings to `v0.1.16`.

#### [MODIFY] [doc/versions.md](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/doc/versions.md)
- Document v0.1.16 release notes under `## [v0.1.0 to v0.1.16] - 2026-09-12`.

#### [MODIFY] [doc/DOCUMENTATION.md](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/doc/DOCUMENTATION.md)
- Record update under `## Version History`.

#### [MODIFY] [README.md](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/README.md)
- Update version badge and features checklist.

#### [NEW] [doc/prompts/1.30 Implementation Plan - Scalable Multi-MCU System Architecture and Authentic NeoPixel Heart Synthesis.md](file:///d:/GitHub/OSS/TCAD-WOKWI-Translator/doc/prompts/1.30%20Implementation%20Plan%20-%20Scalable%20Multi-MCU%20System%20Architecture%20and%20Authentic%20NeoPixel%20Heart%20Synthesis.md)
- Dual-write copy of this implementation plan.

---

## Verification Plan

### Automated Tests
1. Run Node.js test script verifying:
   - Synthesis of circuits with 2, 4, 8, and $N$ Arduino boards.
   - Multi-MCU inter-board wire routing and compilation into valid Wokwi `diagram.json`.
   - NeoPixel strip (8 px) and ring (12 px) catalog registration and pin normalization.
   - Authentic Valentine Contest Entry translation with 4 Arduinos, 6 strips, 3 rings, LCD, and power supply.
   - Standalone HTML export generation and dynamic rendering.

### Manual Verification
- View the 4-Arduino NeoPixel heart layout on canvas.
- Add additional Arduinos from the sidebar palette and verify non-overlapping placement and auto-fit.
- Test export to Wokwi and Standalone HTML.
